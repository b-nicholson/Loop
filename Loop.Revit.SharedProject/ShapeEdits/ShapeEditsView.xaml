<Window
    x:Class="Loop.Revit.ShapeEdits.ShapeEditsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Loop.Revit.ShapeEdits"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button Command="{Binding SelectTarget, Mode=OneWay}" Content="Target"/>
        <Button Command="{Binding SelectHost, Mode=OneWay}" Content="Host" Grid.Row="1"/>
        <Button Command="{Binding Run, Mode=OneWay}" Content="Run" Grid.Row="2" />

    </Grid>
</Window>
